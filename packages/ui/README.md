# @ais-chat/ui

Shared UI component package for the AIS.chat monorepo.

## adding a new shadcn component

When adding a new component, verify the component is detected by shadcn cli:

```bash
pnpx shadcn info -c packages/ui
```
